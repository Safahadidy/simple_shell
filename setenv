#include "shell.h"
exten char **environ
{
int i = 0, len = 0;
char *new_var;
while (environ[i])
{

len = strlen(name);
if (strcmp(environ[i], name, len)
{
if (overwrite)
{
new_var = malloc(strlen(name) + strlen(value) + 2)
strcpy(new_var, name);
strcat(new_var, "=");
strcat(new_var, value);
}
}
i++;
}
new_var = malloc(strlen(name) + strlen(value) + 2)
strcpy(new_var, name);
strcat(new_var, "=");
strcat(new_var, value);
environ[i] = new_var;
environ[i] = new_var;
environ[i + 1] = NULL;
return (0);
}
